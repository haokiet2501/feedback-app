function App() {
    const comments = [
        {id: 1, text: "Comment 1"},
        {id: 2, text: "Comment 2"},
        {id: 2, text: "Comment 3"}
    ]

    const loading = false
    const showComments = false
    if (loading) return <h1>Loading...</h1>
    return (
        <div>
            {showComments && }
        </div>
    )
}

export default App